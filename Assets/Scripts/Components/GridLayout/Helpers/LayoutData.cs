namespace Envast.Components.GridLayout.Helpers
{
    public enum FreeAnchorsTypes
    {
        Undefined, MiddleCenter, TopCenter, BottomCenter, MiddleLeft, TopLeft, TopRight, BottomLeft, BottomRight, MiddleRight
    }

    public enum LayoutElementTypes
    {
        FreeAnchors, LayoutGroup, SideStretched, FullStretched
    }

    public enum Alignment
    {
        UpperLeft,
        UpperCenter,
        UpperRight,
        MiddleLeft,
        MiddleCenter,
        MiddleRight,
        LowerLeft,
        LowerCenter,
        LowerRight
    }

    public enum LayoutType
    {
        RTL, LTR
    }
}